function updateData(){
    gameRef.player = globals.player; //vérifier si ça marche
    //gameRef.xElement already defeated etc...
    gameRef.currentChapter = ""; //string de quelle fonction en est oÙ pour le décor et touT;
}
